# Phase 1 — Target Freeze

Goal:

- freeze target and peptide sequence sources

Record:

- target identifier and source
- peptide references and provenance
- non-canonical handling policy
- query naming convention
- recommended upgrade trigger authority file
- canonical structure format for promoted outputs

Gate:

- the main reference and one negative are defined clearly enough to build the
  reference panel

Definition of done:

- target source, peptide provenance, and query naming convention are all explicit
