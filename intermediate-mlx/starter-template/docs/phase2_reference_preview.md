# Phase 2 — Reference Preview

Goal:

- test preview mode on references before any broad batch

Record:

- panel members
- exact preview settings
- whether positives and negatives separate at all
- whether the result is `PASS`, `SOFT PASS`, or `FAIL`
- what preview mode is allowed to claim next

Gate:

- either the lane is directionally useful, or it is explicitly demoted to
  preview-only with no ranking claims

Definition of done:

- the phase has a recorded `PASS`, `SOFT PASS`, or `FAIL`
- preview claim boundaries are written explicitly
