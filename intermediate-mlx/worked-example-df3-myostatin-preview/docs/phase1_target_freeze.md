# Phase 1 — Target Freeze

Goal:

- define one primary target, two related counter-screens, and one peptide
  reference for a preview panel

Why this example is useful:

- it forces explicit chain order
- it shows how related targets can be used without pretending preview mode is a
  final specificity assay

Frozen choices:

- chain `A` = target
- chain `B` = peptide
- seed `42`
- preview mode only
